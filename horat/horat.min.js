main_app.controller('HomeController', function($scope) {
 
});


main_app.controller('SignInController',['$scope','$http','$location', function($scope, $http, $location) {
	
  $scope.SignIn = function(){
	 
		
		// default post header
		$http.defaults.headers.post['Content-Type'] = 'application/x-www-form-urlencoded;charset=utf-8';
		// send login data
		$http({
			method: 'POST',
			url: 'server/login.asp',
			data: $.param({
				username: $scope.Username,
				password: $scope.Password
			}),
			headers: {'Content-Type': 'application/x-www-form-urlencoded'}
		}).then(function (response, status, headers, config) {
			
			if(response.data.status == 1){
				$location.path("/home");
			}
			else{
				$scope.IsInvalidLoginAttempted = true;
			}
		},function (response, status, headers, config) {
			$scope.IsInvalidLoginAttempted = true;
		});

		
	};
}]);

main_app.controller('ForgotController', function($scope) {
  
});

main_app.controller('UserController', function($scope) {
  
});